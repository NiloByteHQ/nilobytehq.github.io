{
  "name": "nilobyte.github.io",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:css": "tailwindcss -i ./src_css/styles.css -o ./assets/css/styles.css --minify",
    "watch:css": "tailwindcss -i ./src_css/styles.css -o ./assets/css/styles.css --watch",
    "build:11ty": "npx @11ty/eleventy",
    "watch:11ty": "npx @11ty/eleventy --serve --quiet",
    "start": "npm run build:css && npm-run-all --parallel watch:*",
    "build": "npm run build:css && npm run build:11ty"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "autoprefixer": "^10.4.21",
    "npm-run-all": "^4.1.5",
    "postcss": "^8.5.3",
    "tailwindcss": "^4.1.4"
  },
  "dependencies": {
    "luxon": "^3.6.1"
  }
}
